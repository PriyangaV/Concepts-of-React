/*
  @ Installation
    - The latest version of node

    # Hello World of React
      $ npx react-app first-app
      $ npm start
      $ http://localhost:3000

      $ npx react-app first-app
      $ npm init react-app first-app
      $ yarn create react-app first-app

    # Package Managers
      $ React is a collection of many other packages/libraries

      $ Each package has to be managed properly - like updates, versions and more...

      $ A typical react application can contain hundreds of package to run effectively

      $ We need a package manager for this
        ~ NPM and Yarn are the common ones

      $ Being fast on loading, having access to more packages
*/
